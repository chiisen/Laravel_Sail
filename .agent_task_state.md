🚩 目標：在 `http://localhost/dashboard` 新增可展示的 DEMO 報表區塊。
✅ 進展：
- 已完成 `resources/js/Pages/Dashboard.vue`：加入 KPI 卡、營收趨勢、流量來源與專案進度 DEMO。
- 已更新 `CHANGELOG.md`（Unreleased > Added）記錄此次儀表板新增內容。
- 已執行 `npm run build` 驗證前端編譯成功。
🚀 下一步：
- 若需改為真實資料來源，可再把 DEMO 陣列改為 Controller + Inertia props 注入。
